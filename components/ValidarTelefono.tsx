import { FunctionalComponent } from "preact/src/index.d.ts";
import { Phone } from "../utils/api.ts";
import { PageProps } from "$fresh/server.ts";

/*const ValidarTelefono: FunctionalComponent<string> = async(props) => { 
  const telefono = await Phone(props.toString())
  console.log(telefono)
  return (
    <div></div>
  )
}

export default ValidarTelefono*/